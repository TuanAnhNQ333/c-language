#include<stdio.h>
double *maximum(double *a, int size)
{
    int i, int max;
    for(i = 0;i < max; i ++) {
        if(i > max) {
            max = i;
        }
    }
    return max;
}
int main()
{
    
}